# Environment
ENVIRONMENT=development

# Flask
FLASK_APP=app.py
FLASK_DEBUG=True
SECRET_KEY=your-secret-key-here

# Server
HOST=0.0.0.0
PORT=5000

# Logging
LOG_LEVEL=INFO

# Docker (nếu cần)
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# CI/CD Secrets (KHÔNG commit vào git!)
# DOCKER_USERNAME=your-dockerhub-username
# DOCKER_PASSWORD=your-dockerhub-password
